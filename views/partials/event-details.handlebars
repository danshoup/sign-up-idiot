<section class="card">
  <a href="/events/{{id}}">
    <header>{{event_name}}</header>
    <img src="/images/{{images.[0].filename}}" alt="{{images.[0].description}}">
  </a>
  <p>Volunteer Tasks</p>
  {{#each tasks as |task|}}
  {{> task-details}}
  {{/each}}
</section>



